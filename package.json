{
  "name": "nyx-bare",
  "version": "1.0.0",
  "description": "Bare server for Nyx proxy",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@tomphttp/bare-server-node": "^2.0.6",
    "express": "^5.2.1"
  }
}